<Window x:Class="WpfNC1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfNC1"
        mc:Ignorable="d"
        Title="MainWindow" VerticalAlignment="Top" Height="409" Width="762">



        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ShowGridLines="True">
            <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
        <DataGrid x:Name="DataGrid1" ItemsSource="{Binding}"  Grid.Row="1" Grid.Column="1" AutoGenerateColumns="True">
        </DataGrid>
        </Grid>
</Window>
